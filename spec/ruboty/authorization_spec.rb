require 'spec_helper'

describe Ruboty::Authorization do
  it 'has a version number' do
    expect(Ruboty::Authorization::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
